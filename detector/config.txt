# HTTP_Server	: Socket you want to send data to
# TIMEOUT		: How long detector tries to get data before giving up; only effective in laptop mode
# RATE			: How long detectory waits before getting another data point
# LED_gpio 		: GPIO for led, only available on pi
# button_gpio	: button GPIO for button, only available on pi; stops programs
[DEFAULT]
HTTP_Server = http://127.0.0.1:3000
TIMEOUT = 5
RATE = 5
LED_gpio = 3 # GPIO pin for LED 
button_gpio = 23 # GPIO pin for Button
